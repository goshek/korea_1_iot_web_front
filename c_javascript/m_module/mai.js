import {add} from './matho.js'
import { subtract } from './matho.js'
import multiply, * as multi from './matho.js'
import defaultMember, * as alias from "module-name";

let twoNum=[9,77];
console.log(add(twoNum[0], twoNum[1]));
console.log(subtract(twoNum[0], twoNum[1]));
console.log(multi(twoNum[0], twoNum[1]));